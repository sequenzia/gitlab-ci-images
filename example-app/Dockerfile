# Example application image
# This is a sample Dockerfile demonstrating the build pipeline

ARG NODE_VERSION=20

FROM node:${NODE_VERSION}-alpine

LABEL maintainer="your-team@example.com"
LABEL description="Example application image"

WORKDIR /app

# Example: Copy application files (uncomment when you have actual files)
# COPY package*.json ./
# RUN npm ci --only=production
# COPY . .

# Placeholder command
CMD ["echo", "Example app container started"]
